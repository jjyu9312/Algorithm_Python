new_id = "...!@BaT#*..y.abcdefghijklm"

new_id = re.sub("[^a-z0-9\-\_\.]", "", new_id)   

new_id
new_id = new_id.lower()

new_id

chars = "!@#*"

for x in range(len(chars)):
    new_id = new_id.replace(chars[x], '')

new_id     

    
import re

while '..' in new_id:
    new_id = new_id.replace('..', '.')

new_id
news = ""
if len(news) == 0:
    news = "a"

new_id

if new_id.startswith("."):
    new_id = new_id[1:]

print(new_id)

if len(new_id) > 15:
    new_id = new_id[:15]
    
new_id

news = "ab"
if len(news) <= 2:
    k = news[-1] * 1000000
    news = news + k
    print(news[:3])
    
abc = "asdsdq.."


abc[:-1]
